import { TouchControlDirective } from "./touch-control.directive";

describe('TouchControlDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchControlDirective();
    expect(directive).toBeTruthy();
  });
});
