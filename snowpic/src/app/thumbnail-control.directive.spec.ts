import { ThumbnailControlDirective } from './thumbnail-control.directive';

describe('ThumbnailControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbnailControlDirective();
    expect(directive).toBeTruthy();
  });
});
