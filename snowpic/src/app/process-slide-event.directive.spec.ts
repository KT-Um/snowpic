import { ProcessSlideEventDirective } from './process-event.directive';

describe('ProcessSlideEventDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcessEventDirective();
    expect(directive).toBeTruthy();
  });
});
